import root from "../../Feature.module.scss";
import styles from "./Profile.module.scss";

export const Profile = () => {
  return <div className={`${root.feature_container} ${styles.profile}`}></div>;
};
